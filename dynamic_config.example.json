{
  "_meta": {
    "schema_version": 1,
    "description": "pihole-watch tuning. Edit by hand for manual A/B; weekly calibrator overwrites the threshold fields. Detection picks up changes on the next 5-min cycle.",
    "ssot": "this file is the SSOT for current tuning values. calibration_history (in findings.db) is the audit trail.",
    "last_calibrated_at": null
  },
  "lookback_minutes": 6,
  "beacon_lookback_minutes": 60,
  "beacon_min_occurrences": 6,
  "dga_threshold": 0.65,
  "nxdomain_rate_threshold": 0.30,
  "beacon_max_interval_cv": 0.15,
  "volume_sigma_threshold": 3.0,
  "triage": {
    "enabled": false,
    "model": "qwen3:4b",
    "score_min": 0.65,
    "score_max": 0.85,
    "max_per_run": 20,
    "interval_hours": 24,
    "timeout_seconds": 90.0
  }
}
